# 如何维护你的书单（说白了，就是改 `data/分类总览.md` 这一个文件）

> **一句话原则：这个仓库里"你的书单"就是 `data/分类总览.md` 这一个文件。所谓"维护书单"，就是改它。** 卡片、图谱网页（`书籍知识图谱模板.html`）全都从它衍生出来，你不用碰那些产物文件。

> 本仓库不绑定任何 AI 工具：你用 WorkBuddy、Claude、ChatGPT、Cursor 等任意一个能读文件的 AI 助手都行。下面「让 AI 助手帮你重生成」的说法，换成你正在用的工具即可。

## 1. 真值原则

| 角色 | 文件 |
|---|---|
| **唯一真值（你改这一份就行）** | `data/分类总览.md` |
| 已生成的可视化（改 分类总览 后重跑即可） | `书籍知识图谱模板.html` |

**任何文件的冲突都听 `data/分类总览.md` 的**。如果遇到矛盾，先重生成再交付。

---

## 2. 你可以改什么 — `分类总览.md` 行格式

文件里**所有书条目**严格遵循同一行格式（用全角空格 `　` 分隔字段）：

```
数字. 《书名》　作者：xxx　概念词：概念1、概念2、概念3　豆瓣评分：X.X
```

### 字段解释
- **`数字.`** — 在该分类内的序号，每类从 1 起。删书后**号段允许跳号**（例：删了第 7 本后剩 1,2,3,5,6——这正常，无需补回）。
- **`《书名》`** — 书的主标 + 副标，**外层《》不要漏**。书名有歧义/撞名时**保留全名**（如《原则：应对变化中的世界秩序》），不要造括号缩写。
- **`作者：xxx`** — 可多作者用空格或顿号；允许 `[日]` `[美]` `[英]` 等前缀。
- **`概念词：a、b、c`** — 用顿号「、」分隔，2-5 个为佳。**这是图谱连接的源头**，决定你的书会跟哪些书连上线。
- **`豆瓣评分：X.X`** — 0-10，一位小数。如果你没数据，写 `N/A`。

### 你可以做的 6 种编辑

| ✅ 可以 | ❌ 不要 |
|---|---|
| 改书名（连同副标一起改） | 改 15 个分类名 |
| 改正文概念词 | 调换分类间的书（除非明显错归） |
| 改作者署名 | 给书加非通用前缀 `[美]`（除非本来就有） |
| 删不要的书 | 删整本书里的「分类边界说明」/「速查」段 |
| 改豆瓣评分 | 加/改永久避用 3 本（有闲阶级论/看不见的女性/服美役）|
| 调换一本书在分类内的排序 | 改行格式字段顺序（脚本要按固定顺序解析）|

### 改完速查段会自动失效？
- 顶部「速查（15 类 · 共 N 本）」数字是**手写的总结段**，不会跟着书单自动变。
- 你编辑完书单后，**顺手把速查段也刷新一下**（找 AI 助手做这一步最快；详见下文 §5）。

---

## 3. 15 个分类的边界铁律（不要破）

`data/分类总览.md` 顶部那一段「分类边界说明」是硬规则，**不要删**。最重要的 4 条：

1. **思维认知 = 思考总类**（思维/认知/逻辑/心智模型），**不塞能力提升**。
2. **能力提升 = 具体可习得能力**（沟通/写作/阅读/记忆/演讲），**不含"思考本身"**。
3. **人性洞察 = 欲望/阴暗面/进化本能/底层生物心理**，**不含思考类自助书**。
4. **文学经典 = 故事性大类不细分**，不要把《战争与和平》挪去「历史」。

完整细则见 `skills/book-category-classify/SKILL.md` 与 `skills/book-core-points/references/classification_rules.md`。

---

## 4. 改完后怎么重生成图谱 — 三种路径

### 路径 A：自己重生成（推荐有 Python 经验者）

```bash
# 1. 校验 分类总览 文件无格式错误
cd /path/to/book-knowledge-base
python3 <<'PY'
import re
text = open('data/分类总览.md', encoding='utf-8').read()
cat_pat = re.compile(r'^###\s+\d+\.\s*【(.+?)】')
book_pat = re.compile(r'^(\d+)\.\s*《(.+?)》\s*作者[:：](.*?)\s*概念词[:：](.*?)\s*豆瓣评分[:：](\S+)\s*$')
books = [m for m in (book_pat.match(l) for l in text.split('\n')) if m]
cats = [m for m in (cat_pat.match(l) for l in text.split('\n')) if m]
print(f"分类 {len(cats)} 个，书本 {len(books)} 本，格式异常 0")
PY

# 2. 重生成图谱（以当前 书籍知识图谱模板.html 作 baseline，自动处理删书/改名/加书）
python3 scripts/rebuild_final.py --write

# 3. 三道关校验
node --check <(grep -oP '(?<=<script>).*?(?=</script>)' 书籍知识图谱模板.html)  # 语法
grep -c '"type":"book"' 书籍知识图谱模板.html                                  # 期望 = 你 分类总览 书本数
# （用浏览器双击打开，看概念贴书不漂、选中分类能聚拢）
```

### 路径 B：让 AI 助手帮你重生成（推荐大多数人）

直接把话甩给你的 AI 助手：

> 「我刚改了 `data/分类总览.md`（删了 X 本、加了 Y 本、把 Z 改名了）。麻烦基于最新的 分类总览 重新生成一份 `书籍知识图谱模板.html`，沿用现有的图谱样式，跑三道关校验，告诉我书数对得上、没爆错。」

AI 会自动：
- 读最新 分类总览 文件、按规则解析
- 跑重建脚本（保留内嵌数据 + 力代码）
- 校验数据规模、语法、力代码完整
- 输出新文件 + 给你 md5 + 简要数据

### 路径 C：完全不动手（找人帮你维护）

把所有书单编辑 + 让你「看起来能跑起来」的需求打包扔给负责人。说清楚：「我改了 分类总览 哪些行，你看下要不要再加几本，或者说我哪里写错了」。

---

## 5. 兜底：出错了怎么办

### 5.1 一键回到上一版（重生成前必做）

重生成前**永远备份当前生效版**：

```bash
cp 书籍知识图谱模板.html index-pre-改前.html
```

回滚（万一新版空白/报错/书数不对）：

```bash
cp index-pre-改前.html 书籍知识图谱模板.html
```

### 5.2 三类常见错误的肉眼判断

| 现象 | 大概率原因 | 修法 |
|---|---|---|
| 打开 HTML 后画布全空 | JS 语法错 / 引用文件路径错 | 跑 `node --check`；看浏览器 console |
| 书本总数 = 你删之前的数 | `书籍知识图谱模板.html` 不是最新的，重生成 | 跑 §4 重生成路径 |
| 某本书孤零零没邻居 | `概念词` 留空了或写错分类 | 检查 分类总览 那行的概念词字段 |

### 5.3 永久禁手（破了就再也救不回来）

- **不要直接编辑 `书籍知识图谱模板.html` 内嵌的数据段（`_EMBEDDED_GRAPH`）** —— 它是 JSON 字面量，改完即使能渲染也跟 分类总览 对不上，下一次重生成会把你的改动抹掉。
- **不要删 `data/分类总览.md` 顶部的「分类边界说明」段** —— 那段是分类铁律，删了下次重生成概念词映射会乱。
- **不要把 15 个分类改名** —— 颜色配色、图例、概念节点的归属都绑死分类名。

---

## 6. 完整工作流（推荐流程）

```
1. Read data/分类总览.md
        ↓
   你觉得哪本不要了 / 想加一本 / 书名改了
        ↓
2. Edit data/分类总览.md（按 §2 行格式）
        ↓
3. 刷新速查段（§5 末尾提示的数字 + 各分类本数）
        ↓
4. 让 AI 助手按 §4 路径 B 重生成 书籍知识图谱模板.html
        ↓
5. 浏览器打开看一眼：书数对、概念贴书不飘、选中分类能聚拢
        ↓
6. 留着旧版做 pre-改前备份，新版立刻生效
```

**少出问题的核心 = 只改 分类总览，其他都让脚本/AI 重生成。**
