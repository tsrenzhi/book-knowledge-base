# 更新日志

本文件记录书籍知识库（book-knowledge-base）的关键变更。约定：
- **新增 / 变更 / 弃用 / 修复** 四类标签
- 每次发布的最小可重放单元：commit 哈希 + 改了什么 + 为什么
- 读完这条，你应能**回滚到任意一个历史版本**或**理解当前状态如何从过去长成**

---

## [unreleased] · 模板化开源

> 本次变更将本仓库从「个人阅读清单」转为「可复用模板」。

### 变更
- **数据文件改名**：`data/分类总览-v8.md` → `data/books-catalog.md` → `data/分类总览.md`（去除内部版本号与英文代号，文件名即语义，最终定名见下方本轮追加变更）
- **示例书收敛到 6 本**（之前 13 本）：国富论 / 纳瓦尔宝典 / 列奥纳多·达·芬奇传 / 人类简史 / 富爸爸穷爸爸 / 孙子兵法
- **图片资源不公开**：6 本示例书的知识卡片 `.png` 预览图、达芬奇传 `assets/` 内插图全部移除（让用户用 skill 自己生成）
- **全量图谱保留为演示版**：`index.html` 仍含完整 426 本书数据，README 标注「演示版」——用户可参考其结构，但**真实书单由用户自己维护**
- **机读副本命名统一**：`v8-core.json` → `分类总览.json`（在文档/脚本引用里同步替换）
- **脚本去绝对路径**：`scripts/rebuild_final.py` 不再写死本地绝对路径，改为相对仓库根目录
- **新增 `LICENSE`（MIT）**：允许商用修改
- **新增 `init.sh`**：clone 后一键把 `skills/` 拷到 `~/.workbuddy/skills/`
- **`.gitignore` 加固**：加 `*.pre-*`，防备份文件再被误入库

### 本轮追加变更（中文化与多工具适配）
- **数据 / 文档文件名中文化**：`data/books-catalog.md` → `data/分类总览.md`、`CHANGELOG.md` → `更新记录.md`，所有引用（脚本 / 文档 / `init.sh`）同步更新；`rebuild_final.py` 干跑验证 426 本图谱仍对齐、悬空连线 0
- **README / 操作手册改为工具无关**：去掉「只能 WorkBuddy」的限定，新增「怎么开始用（不绑定任何 AI 工具）」章节，说明 WorkBuddy 与其他 AI 工具（Claude / ChatGPT / Cursor 等）都能用；操作手册「背后技能」一节点名 5 个 skill 及各自解决的问题
- **README 新增能力清单**：用表格列出 `book-knowledge-card` / `book-core-points` / `book-category-classify` / `book-knowledge-graph` / `book-knowledge-workflow` 五个技能各自解决什么、干什么
- **skills 全面去个人路径**：所有 `~/WorkBuddy/books/`、`~/.workbuddy/skills/` 泛化为「你的书籍库 / 本仓库 skills 目录」；graph 辅助脚本的 `books_dir` 默认改为读 `BOOKS_DIR` 环境变量、回退到当前目录下的 `books/`，非 WorkBuddy 用户也能跑
- **示例卡 frontmatter 去绝对路径**：6 张示例卡 md 的 `source_card` 改为仓库内相对路径

### 模板化约定
- **6 本书是「如何做」的范例**——不是「必须做」的清单。用户 clone 后，照着 skill 流程能做出自己的卡片/图谱
- **全量 426 本是「做到了什么」的演示**——结构、字段、连线、视觉风格可参考，但**不是默认数据**
- **用户从空书单起步**：`data/分类总览.md` 在本地起手时按需加自己的书，不复制示例书

### 迁移指南（已 clone 老版本的用户）
1. `git pull` 拉新版本
2. 重命名：`git mv data/books-catalog.md data/分类总览.md`（如本地有改）
3. 跑 `bash init.sh` 重新装配 skills
4. 删掉本地 `cards/` 里不在 6 本示例里的书（它们已从仓库移除）
5. 检查 `scripts/rebuild_final.py` 的路径配置，按 README「重建图谱」章节重生成

---

## 历史里程碑（迁移前速记）

| 版本 | 关键节点 | 备注 |
|---|---|---|
| 早期 | 手写卡片 md + 自渲染 HTML | 13 本示例书成型 |
| v8 | 引入「分类总览-v8.md」真值 + 4 skill 串联 | 426 本全量图谱落地 |
| v9 | 模板化开源（本版本） | 6 本示例 + 演示图谱，去 V8 字样 |
